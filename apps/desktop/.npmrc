# Copy `file:` dependencies into node_modules (honouring the package's `files`
# allowlist) instead of symlinking the whole repo root. This keeps the packaged
# app lean: only local-llmup's published surface (dist + data) and its runtime
# deps are pulled in, never the repo's docs/tests/coverage or dev tooling.
install-links=true
